README
======

To apply code formatting to a file you are working on, currently you can do this manually:

This will edit the file, and print to stdout:
   
  clang-format [file]

This will edit the file in place, do this if you are sure of what you are doing:

  clang-format -i [file]
